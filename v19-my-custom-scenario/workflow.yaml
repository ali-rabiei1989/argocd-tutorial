apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-workflow-
spec:
  entrypoint: main
  templates:
    - name: main
      container:
        image: busybox
        command:
          - sh
          - -c
        args:
          - echo "Hello from Argo Workflows"
